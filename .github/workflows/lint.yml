name: Lint & Format Check

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

jobs:
  lint-format:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Add this line to grant write permission

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Auto-fix ESLint issues
        run: |
          npm run lint:fix
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "chore: auto-fix lint issues" || echo "No lint fixes"
          git checkout -b lint-fixes || git checkout lint-fixes
          git pull origin lint-fixes  # Pull the latest changes
          git push origin lint-fixes

      - name: Check Prettier formatting
        run: npm run format:check

      - name: Auto-fix Prettier issues
        run: |
          npm run format:fix
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "chore: auto-fix format issues" || echo "No format fixes"
          git checkout -b format-fixes || git checkout format-fixes
          git pull origin format-fixes  # Pull the latest changes
          git push origin format-fixes